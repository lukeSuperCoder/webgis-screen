# WebGIS呼市水质监测系统 - 未完成前后端联调功能清单

## 项目概述
本项目是一个基于Vue.js和OpenLayers的WebGIS水质监测系统，目前前端已完成基础功能开发，但部分功能仍使用静态数据进行展示，需要与后端API进行联调。

## 静态数据文件说明
项目中使用的主要静态数据文件：
- `src/assets/data/well_data.json` - 监测井数据（包含60个监测井的坐标和监测指标数据）
- `src/assets/data/bianjie.json` - 项目边界数据（GeoJSON格式）
- `src/assets/data/156150700.json` - 呼伦贝尔市行政区划数据（GeoJSON格式）

## 已定义的后端API接口

### 1. 认证管理 (`src/api/auth.js`)
- ✅ `POST /login` - 用户登录
- ✅ `GET /getInfo` - 获取用户信息
- ✅ `GET /getRouters` - 获取路由信息
- ✅ `POST /logout` - 用户登出

### 2. 用户管理 (`src/api/user.js`)
- ✅ `GET /system/user/list` - 获取用户列表
- ✅ `POST /system/user` - 新增用户
- ✅ `PUT /system/user` - 修改用户
- ✅ `DELETE /system/user/{userIds}` - 删除用户
- ✅ `GET /system/user/{userId}` - 获取用户详情
- ✅ `PUT /system/user/changeStatus` - 修改用户状态
- ✅ `PUT /system/user/resetPwd` - 重置用户密码
- ✅ `GET /system/user/deptTree` - 获取部门树列表
- ✅ `POST /system/user/export` - 导出用户列表
- ✅ `POST /system/user/importData` - 导入用户数据
- ✅ `POST /system/user/importTemplate` - 下载导入模板

### 3. 监测井管理 (`src/api/monitorWell.js`)
- ✅ `GET /monitor/well/spatial` - 监测井空间查询
- ✅ `GET /monitor/well/list` - 获取监测井列表
- ✅ `GET /monitor/well/{wellCode}` - 根据井编码查询监测井详细信息
- ✅ `POST /monitor/well` - 添加监测井
- ✅ `PUT /monitor/well` - 更新监测井
- ✅ `DELETE /monitor/well/wellCodes` - 删除监测井
- ✅ `POST /monitor/well/import` - 导入监测井数据

### 4. 监测数据管理 (`src/api/monitorData.js`)
- ✅ `GET /monitor/sample/list` - 获取监测样本列表
- ✅ `POST /monitor/sample/import` - 导入监测样本数据
- ✅ `GET /monitor/sample/metrics` - 获取监测样本指标
- ✅ `GET /monitor/sample/data` - 获取监测数据
- ✅ `GET /monitor/sample/data/range` - 获取监测数据范围
- ✅ `GET /monitor/metric/list` - 获取检测指标列表
- ✅ `POST /monitor/metric` - 添加检测指标
- ✅ `PUT /monitor/metric` - 更新检测指标
- ✅ `DELETE /monitor/metric` - 删除检测指标

### 5. 项目管理 (`src/api/project.js`)
- ✅ `GET /project/list` - 获取项目列表
- ✅ `GET /project/{projectId}` - 获取项目详情
- ✅ `POST /project` - 添加项目
- ✅ `PUT /project` - 更新项目
- ✅ `DELETE /project` - 删除项目
- ✅ `GET /project/companyNames` - 获取公司名称列表
- ✅ `POST /project/importShp` - 导入Shapefile数据

### 6. 检测指标管理 (`src/api/qualityMetric.js`)
- ✅ `GET /monitor/metric/list` - 分页查询指标标准
- ✅ `POST /monitor/metric` - 新增指标标准
- ✅ `PUT /monitor/metric` - 修改指标标准
- ✅ `DELETE /monitor/metric/{ids}` - 删除指标标准
- ✅ `GET /monitor/metric/codes` - 获取指标编码列表

## 未完成前后端联调的功能清单

### 🔴 高优先级 - 核心功能

#### 1. 地图监测页面 (`src/views/Monitor.vue`)
**当前状态：** 使用静态数据 `well_data.json`
**需要联调的功能：**
- [ ] **监测井空间查询** - 调用 `getMonitorWellSpatial()` API
- [ ] **综合水质分布** - 需要后端提供实时监测数据
- [ ] **单项水质分布（pH、总磷）** - 需要后端提供单项指标数据
- [ ] **监测井详细信息** - 调用 `getMonitorWellInfo()` API

**影响文件：**
- `src/views/Monitor.vue` (第52行、104行、113行使用静态数据)
- `src/components/ScreenMap.vue` (地图组件)

#### 2. 监测数据管理 (`src/views/data-manage/monitoring-data.vue`)
**当前状态：** 已调用API但可能数据格式不匹配
**需要联调的功能：**
- [ ] **监测数据列表查询** - `getSampleList()` API数据格式验证
- [ ] **监测数据导入** - `importSampleData()` API功能测试
- [ ] **监测数据导出** - 后端导出功能开发

#### 3. 监测井数据管理 (`src/views/data-manage/well-management.vue`)
**当前状态：** 已调用API但可能数据格式不匹配
**需要联调的功能：**
- [ ] **监测井列表查询** - `getMonitorWellList()` API数据格式验证
- [ ] **监测井CRUD操作** - 增删改查功能测试
- [ ] **监测井导入** - `importMonitorWell()` API功能测试
- [ ] **监测井导出** - 后端导出功能开发

### 🟡 中优先级 - 管理功能

#### 4. 项目管理 (`src/views/data-manage/project-config.vue`)
**当前状态：** 已调用API但可能数据格式不匹配
**需要联调的功能：**
- [ ] **项目列表查询** - `getProjectList()` API数据格式验证
- [ ] **项目CRUD操作** - 增删改查功能测试
- [ ] **Shapefile导入** - `importShpData()` API功能测试
- [ ] **公司名称查询** - `getCompanyNames()` API功能测试

#### 5. 数据质控规则 (`src/views/data-manage/quality-rules.vue`)
**当前状态：** 已调用API但可能数据格式不匹配
**需要联调的功能：**
- [ ] **指标标准查询** - `getMetricList()` API数据格式验证
- [ ] **指标标准CRUD操作** - 增删改查功能测试
- [ ] **指标编码查询** - `getMetricCodes()` API功能测试

#### 6. 综合分析页面 (`src/views/analysis/comprehensive.vue`)
**当前状态：** 使用硬编码的静态数据
**需要联调的功能：**
- [ ] **监测数据查询** - 需要后端提供综合分析数据
- [ ] **数据统计功能** - 需要后端提供统计计算

### 🟢 低优先级 - 系统管理

#### 7. 用户管理 (`src/views/system-manage/user-management.vue`)
**当前状态：** 已调用API但可能数据格式不匹配
**需要联调的功能：**
- [ ] **用户列表查询** - `getUserList()` API数据格式验证
- [ ] **用户CRUD操作** - 增删改查功能测试
- [ ] **用户导入导出** - 相关API功能测试
- [ ] **部门树查询** - `getDeptTree()` API功能测试

#### 8. 系统管理其他功能
**需要联调的功能：**
- [ ] **角色管理** - `src/api/systemRole.js` 相关API
- [ ] **部门管理** - `src/api/systemDept.js` 相关API
- [ ] **数据字典管理** - `src/api/systemDict.js` 相关API

## 联调注意事项

### 1. 数据格式要求
- **监测井数据格式：** 需要包含 `wellCode`、`longitude`、`latitude`、`geom` 等字段
- **监测数据格式：** 需要包含 `monitoringWellCode`、`metricName`、`value`、`samplingTime` 等字段
- **空间数据格式：** 支持WKT格式的几何数据

### 2. 分页参数
- 统一使用 `pageNum` 和 `pageSize` 参数
- 返回格式：`{ code: 200, rows: [], total: 0 }`

### 3. 文件上传
- 支持 `.xlsx`、`.xls` 格式
- 使用 `multipart/form-data` 格式上传

### 4. 错误处理
- 统一错误码：200表示成功，其他表示失败
- 错误信息通过 `msg` 字段返回

## 建议的联调顺序

1. **第一阶段：** 认证和用户管理功能
2. **第二阶段：** 监测井和监测数据的基础CRUD操作
3. **第三阶段：** 地图展示和空间查询功能
4. **第四阶段：** 数据导入导出功能
5. **第五阶段：** 高级分析和统计功能

## 测试建议

1. **单元测试：** 每个API接口的单独测试
2. **集成测试：** 前后端联调测试
3. **性能测试：** 大数据量下的查询性能
4. **兼容性测试：** 不同浏览器的兼容性

---

**文档更新时间：** 2025年1月
**文档版本：** v1.0
**维护人员：** 开发团队
